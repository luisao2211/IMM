export interface Authentication {
    name: string;
    email: string;
    password: string;
  }
export interface Login {
    id:number,
    token:string,
}
export interface Emails {
  email:string,
}
